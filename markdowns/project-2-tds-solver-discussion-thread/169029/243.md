---
post_url: https://discourse.onlinedegree.iitm.ac.in/t/project-2-tds-solver-discussion-thread/169029/243
---
[@Saransh\_Saini](/u/saransh_saini) Is answer in this form acceptable..  

```markdown
## Image Description

The image displays a code snippet that illustrates how to calculate cosine similarity between pairs of embeddings using Python. 

### Key Components:

- **cURL Command:** 
  - Makes a POST request to the URL `http://127.0.0.1:8000/api/`.
  - Specifies the content type as `application/json`.

- **Request Body:**
  - Contains a query about the company "ShopSmart," which values customer feedback and receives numerous comments monthly.

- **Server Response:**
  - Includes a `200` status code indicating success.
  - The response body contains a Python function that computes the cosine similarity between phrases.
  - Displays an example with embeddings corresponding to feedback phrases, such as "Packing was excellent!" and "Shipping costs were too high."

### Code Features:
- Usage of libraries like `numpy` for numerical operations.
- Functions for calculating similarities and retrieving pairs with the highest cosine similarity values.

### Format:
- The response is formatted in JSON with key-value pairs.
```