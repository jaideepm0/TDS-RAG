```markdown
### Image Description

The image displays a terminal or command prompt executing a Python server process using Uvicorn. The relevant commands and outputs are as follows:

- The current directory is `C:\IIT_Madras\TDS_Project`.
- A Uvicorn command is run with the following parameters:
  - `app:app`  as the application to serve
  - `--host 127.0.0.1` specifying the host IP
  - `--port 8000` indicating the port number

### Output Log

- **INFO**: Finished server process [30576]
- **INFO**: Started server process [5584]
- **INFO**: Waiting for application startup.
- **INFO**: Application startup complete.
- **INFO**: Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
- **INFO**: 127.0.0.1:54184 - "GET / HTTP/1.1" 200 OK
```